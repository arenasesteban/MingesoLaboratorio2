server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vehicle-db
    username: postgres
    password: 12345678
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka